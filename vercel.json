{
  "buildCommand": "npm run build",
  "outputDirectory": "frontend/dist",
  "devCommand": "cd frontend && npm run dev -- --port $PORT",
  "framework": "vite",
"routes": [
    {
      "src": "^/(?!api/)(?!.*\\.).*$",
      "dest": "/index.html"
    }
  ]
}
