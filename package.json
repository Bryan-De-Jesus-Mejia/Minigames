{
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "test": "vitest run",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "bootstrap-icons": "^1.13.1",
    "googleapis": "^144.0.0"
  },
  "devDependencies": {
    "@vercel/node": "^5.0.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
