{
  "scripts": {
    "dev": "cd Minigames && npm run dev",
    "build": "cd Minigames && npm run build",
    "install": "cd Minigames && npm install",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "bootstrap-icons": "^1.13.1"
  }
}
